import React, { Component } from 'react';

// includes

class Header extends Component {
  render() {
    return (
      <header>
          Test Header
      </header>
    );
  }
}

export default Header;
